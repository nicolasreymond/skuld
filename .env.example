# Interface d'écoute de l'API skuld (interpolée par docker-compose).
# 127.0.0.1 = localhost seulement. Pour un accès depuis le téléphone via Tailscale,
# mets l'IP Tailscale de CETTE machine.
SKULD_BIND=127.0.0.1
