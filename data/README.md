# data/

- `horaire.ics` : ton emploi du temps exporté de GAPS
  (Horaires → « Télécharger cet horaire au format Outlook et Apple Calendar »).
  Place-le ici sous le nom `horaire.ics`.
- `history/grades.json` : écrit automatiquement par le scraper (ne pas committer).
